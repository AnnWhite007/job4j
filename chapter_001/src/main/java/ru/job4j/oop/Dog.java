package ru.job4j.oop;

/**
 * 1.2.1.1. Создание объекта
 */
public class Dog {
    public static void main(String[] args) {
        //создаем переменную типа Dog с именем polkan...
        //Оператор new резервирует кусок памяти и записывает в нее данные объекта класса Dog.
        Dog polkan = new Dog();
        Dog sharik = new Dog();
        Dog zhychka = new Dog();
    }
}
